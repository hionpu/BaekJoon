#include <iostream>   

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	int t, w;
	cin >> t >> w;

	int dp[2][1001][32] = { 0 };

	int arr[1001] = { 0 };

	for (int i = 1; i <= t; i++)
	{
		cin >> arr[i];
	}

	for (int i = 1; i <= t; i++)
	{

	}
}

