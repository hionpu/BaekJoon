#include <iostream>
#include <vector>
using namespace std;

struct medu {
    int y;
    int x;
    int d;
};

struct war {
    int y;
    int x;
    bool alive;
};

int main() {
    int n, m; cin >> n >> m;

    vector<vector<int>> town;

    int sr, sc, er, ec; cin >> sr >> sc >> er >> ec;

    for (int i = 0; i < m; ++i)
    {
        int y, x; cin >> y >> x;
    }


    // med move

    // med view

    // warrior move 1

    // warrior move 2

    // warrior attack

    // for each turn, 
    // sum of warrior's move dist 
    // stoned warrior
    // attacked warrior
    return 0;
}